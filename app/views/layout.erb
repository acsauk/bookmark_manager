<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if current_user == "invalid sign-up" %>
      Please re-enter sign-up information and ensure passwords match!
    <% elsif current_user %>
      Welcome, <%= current_user.email %>
    <% end %>
    <%= yield %>  <!-- this then yields to the view which has been called by your controller -->
  </body>
</html>
